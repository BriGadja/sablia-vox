# 2026-04-12 — Sablia Vox

## Session Learnings
- [webapp] Lighthouse Chrome DevTools MCP tool excludes Performance score — use trace-based metrics (LCP, CLS, TTFB) instead. Full Lighthouse CI needed for Performance score — discovered in vox-saas-design-system phase A
- [webapp] Landing page LCP 2,646ms is render-delay dominated (99.5%) — text LCP element with no network dependency, improvement requires JS bundle optimization — discovered in vox-saas-design-system phase A
- [webapp] Dashboard has 4 a11y failures (list structure, label associations, select names) — structurally fixable in Unit 3 sidebar/settings work — discovered in vox-saas-design-system phase A
- [webapp] package.json had next@^16.2.4 but 16.2.4 doesn't exist in npm (only 16.2.3 installed) — blocking all npm install operations. Fixed to ^16.2.3 — discovered in vox-saas-auth-settings phase A
- [webapp] Supabase PostgREST returns joined single-FK relations as arrays in TypeScript types, requiring `as unknown as T[]` cast — discovered in vox-saas-auth-settings phase C

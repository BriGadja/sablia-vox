# 2026-04-17

## Session Learnings

- **Debug dropdown empty in prod (admin impersonation)** — Vercel env var `SUPABASE_SERVICE_ROLE_KEY` was set to an empty string. `createClient(url, '')` doesn't crash; PostgREST just responds with `{"message":"Invalid API key"}`. Routes that swallowed the error into `data ?? []` showed silently empty results. Fix: replaced the value with the v2 sablia-voice service_role JWT, hardened `lib/supabase/admin.ts` to throw at call time if env vars are missing/empty, and turned `.select()` errors in `/api/admin/users` into 500s instead of empty-success responses. Leftover from v1→v2 Supabase migration — the URL was updated but the key was left blank. Commits: `bf6a523` (debug trace), `a729e66` (fix + hardening). Vercel env check: `vercel env pull --environment production` and inspect values, `vercel env ls` only shows names.
